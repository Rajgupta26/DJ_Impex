node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
.vercel
next-env.d.ts
/playwright-report
/test-results
.claude/

# TypeScript incremental build cache: a build artefact that conflicts on
# every merge between two people. Regenerated by `npm run typecheck`.
tsconfig.tsbuildinfo

# Uploads written by the admin panel at /admin/images. The files are tracked so
# that what is deployed matches what the panel shows; only the placeholder keeps
# the empty directory in git.
!public/uploads/.gitkeep
